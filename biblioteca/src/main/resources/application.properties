spring.application.name=biblioteca

spring.datasource.url=jdbc:mysql://localhost:3306/meuappjavaspring?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
# Certifique-se de que estas credenciais batem com o seu MySQL local
spring.datasource.username=root
spring.datasource.password=laila123

#Ensures Hibernate automatically creates or updates the database schema based on your entity mappings.
spring.jpa.hibernate.ddl-auto=update
#Enables logging of SQL queries to the console, useful for debugging.
spring.jpa.show-sql=true